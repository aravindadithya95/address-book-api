# flask configuration
FLASK_CONFIG = {
    'host': 'localhost',
    'port': 5000
}

# elasticsearch configuration
ELASTICSEARCH_CONFIG = {
    'hosts': [
        {
            'host': 'localhost',
            'port': 9200
        }
    ]
}